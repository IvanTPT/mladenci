---
const { data, datumVencanja } = Astro.props;
const qrCodeUrl = `https://api.qrserver.com/v1/create-qr-code/?size=200x200&data=${encodeURIComponent(data.album_url)}`;
---

<section class="section alt-bg">
  <div class="grid grid-cols-1 md:grid-cols-2 gap-8 max-w-6xl mx-auto px-4">
    
    <!-- Лева страна: RSVP -->
    <div class="card p-8 rounded-xl shadow-lg bg-white">
      <h2 class="text-3xl mb-6 font-display text-center">Потврда доласка</h2>
      <!-- Овде убацујеш постојећи HTML/JS код за RSVP форму који смо раније направили -->
<div class="card rsvp-container">
  <h2 id="form-title">Потврда доласка</h2>
  
  <form id="rsvp-form" class="rsvp-form">
    <fieldset>
      <legend>Ваши подаци</legend>
      <div class="input-group">
        <label for="firstName">Име</label>
        <input type="text" id="firstName" name="firstName" required placeholder="Унесите ваше име" />
      </div>
      <div class="input-group">
        <label for="lastName">Презиме</label>
        <input type="text" id="lastName" name="lastName" required placeholder="Унесите ваше презиме" />
      </div>
    </fieldset>

    <fieldset>
      <legend>Особе које долазе са вама</legend>
      <div id="guests-container"></div>
      <button type="button" id="add-guest-btn" class="secondary-btn">
        + Додај особу
      </button>
    </fieldset>

    <button type="submit" id="submit-btn" class="primary-btn">Потврди долазак</button>
  </form>

  <div id="success-message" class="success-message" style="display: none;">
    <svg xmlns="http://www.w3.org/2000/svg" class="success-icon" viewBox="0 0 20 20" fill="currentColor">
      <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd" />
    </svg>
    <h3>Хвала Вам што сте потврдили свој долазак, радујемо се скором виђењу.</h3>
  </div>
</div>

<script>
  // ... (Овај део скрипте остаје ПОТПУНО ИСТИ као у претходном одговору) ...
  const addGuestBtn = document.getElementById('add-guest-btn');
  const guestsContainer = document.getElementById('guests-container');
  const rsvpForm = document.getElementById('rsvp-form');
  const submitBtn = document.getElementById('submit-btn');
  const formTitle = document.getElementById('form-title');
  const successMessage = document.getElementById('success-message');

  let guestCount = 0;

  addGuestBtn?.addEventListener('click', () => {
    guestCount++;
    const guestDiv = document.createElement('div');
    guestDiv.className = 'guest-row';
    guestDiv.innerHTML = `
      <div class="guest-input-group">
        <label>Име пратиоца ${guestCount}</label>
        <input type="text" name="guestFirstName[]" placeholder="Унесите име" required class="guest-input" />
      </div>
      <div class="guest-input-group">
        <label>Презиме пратиоца ${guestCount}</label>
        <input type="text" name="guestLastName[]" placeholder="Унесите презиме" required class="guest-input" />
      </div>
      <button type="button" class="remove-btn" title="Уклони особу">✖</button>
    `;

    guestDiv.querySelector('.remove-btn')?.addEventListener('click', () => {
      guestDiv.remove();
    });
    guestsContainer?.appendChild(guestDiv);
  });

  rsvpForm?.addEventListener('submit', async (e) => {
    e.preventDefault();
    if(submitBtn) submitBtn.textContent = 'Шаљем...';
    
    // ОВДЕ ТИ ИДЕ ТВOЈ GOOGLE SCRIPT ЛИНК
    const GOOGLE_SCRIPT_URL = 'https://script.google.com/macros/s/AKfycbx8izZ0PWNXTl0RREHobK6P3b6NdixRGAnj9dpaTmzQfMtKSY6oZH8AIRiTDKhxemAG/exec'; 
    // Овде иде логика за скупљање података и fetch као из претходног кода...
    // Да би било краће у одговору нисам копирао све, али ту логику не дираш!
    
    setTimeout(() => {
      rsvpForm.style.display = 'none';
      if(formTitle) formTitle.style.display = 'none';
      if(successMessage) successMessage.style.display = 'flex';
    }, 1000);
  });
</script>

<style>
  /* Прилагођено твом глобалном дизајну */
  .rsvp-container {
    width: 100%;
    padding: 2.5rem 2rem;
    border-radius: 1rem;
    box-sizing: border-box;
  }

  h2 {
    text-align: center;
    margin-bottom: 2rem;
    color: var(--color-text-dark);
  }

  fieldset { border: none; padding: 0; margin: 0 0 1.5rem 0; }
  legend { font-weight: 600; margin-bottom: 1rem; color: var(--color-primary); font-size: 1.1rem; }
  
  .input-group { display: flex; flex-direction: column; margin-bottom: 1rem; }
  .input-group label { margin-bottom: 0.5rem; font-size: 0.9rem; color: var(--color-primary); }
  
  input { 
    padding: 0.75rem; 
    border: 1px solid color-mix(in srgb, var(--color-primary), transparent 70%);
    background-color: color-mix(in srgb, var(--color-background), white 50%);
    border-radius: 0.5rem; 
    font-size: 1rem;
    color: var(--color-text-dark);
    outline: none;
    transition: border-color 0.2s;
  }
  input:focus {
    border-color: var(--color-accent);
  }
  
  button { cursor: pointer; font-weight: 600; border: none; border-radius: 0.5rem; transition: all 0.3s ease; }
  
  .primary-btn { 
    width: 100%; 
    padding: 1rem; 
    background-color: var(--color-primary); 
    color: var(--color-background); 
    font-size: 1.1rem; 
    margin-top: 1rem;
  }
  .primary-btn:hover { background-color: var(--color-text-dark); transform: translateY(-2px); }
  
  .secondary-btn { 
    padding: 0.5rem 1rem; 
    background-color: var(--color-secondary); 
    color: var(--color-primary); 
  }
  .secondary-btn:hover { background-color: color-mix(in srgb, var(--color-secondary), black 10%); }

  .success-message {
    flex-direction: column;
    align-items: center;
    justify-content: center;
    text-align: center;
    padding: 2rem 0;
  }
  .success-icon { width: 60px; height: 60px; color: var(--color-accent); margin-bottom: 1rem; }
  .success-message h3 { color: var(--color-primary); }
</style>

<style is:global>
  /* Динамички елементи сада прате твоје боје */
  .guest-row {
    display: flex; gap: 1rem; align-items: flex-end; margin-bottom: 1rem;
    background: color-mix(in srgb, var(--color-background), white 30%);
    padding: 1rem; border-radius: 0.5rem;
    border: 1px dashed color-mix(in srgb, var(--color-primary), transparent 60%);
  }
  .guest-input-group { display: flex; flex-direction: column; flex: 1; }
  .guest-input-group label { margin-bottom: 0.5rem; font-size: 0.85rem; font-weight: 600; color: var(--color-primary); }
  .guest-input { padding: 0.75rem; border: 1px solid color-mix(in srgb, var(--color-primary), transparent 70%); border-radius: 0.5rem; width: 100%; box-sizing: border-box; }
  
  .remove-btn { 
    background-color: transparent; 
    color: var(--color-accent); 
    border: 1px solid var(--color-accent);
    padding: 0.75rem 1rem; display: flex; align-items: center; justify-content: center; height: 42px; 
    border-radius: 0.5rem;
  }
  .remove-btn:hover { background-color: var(--color-accent); color: white; }
</style>
      <!-- ... -->
    </div>

    <!-- Десна страна: Фотографије (са логиком) -->
    <div class="card p-8 rounded-xl shadow-lg bg-white text-center flex flex-col justify-center items-center">
      <h2 class="text-3xl mb-6 font-display">Поделите успомене</h2>
      
      <!-- Контејнери које JS контролише -->
      <div id="photo-message-container">
        <p class="text-lg text-gray-600 mb-4">{data.poruka_pre_vremena}</p>
        <div class="opacity-50 blur-sm pointer-events-none">
           <img src={qrCodeUrl} alt="Замагљен QR код" class="mx-auto border p-2 rounded" />
        </div>
      </div>

      <div id="photo-qr-container" style="display: none;">
        <p class="text-lg text-gray-600 mb-4">Скенирајте QR код и додајте ваше фотографије!</p>
        <img src={qrCodeUrl} alt="QR код за албум" class="mx-auto border-2 border-accent p-2 rounded-xl hover:scale-105 transition" />
        <a href={data.album_url} target="_blank" class="mt-4 inline-block text-accent underline">Или кликните овде</a>
      </div>
    </div>
    
  </div>
</section>

<!-- Прослеђујемо датум венчања у JS преко дата атрибута -->
<div id="wedding-date-data" data-date={datumVencanja} style="display: none;"></div>

<script>
  const targetDateStr = document.getElementById('wedding-date-data')?.dataset.date;
  if(targetDateStr) {
    const targetDate = new Date(targetDateStr).getTime();
    const now = new Date().getTime();

    // Ако је тренутно време веће или једнако датуму венчања (почео је дан венчања)
    if(now >= targetDate) {
      const msgContainer = document.getElementById('photo-message-container');
      const qrContainer = document.getElementById('photo-qr-container');
      
      if(msgContainer && qrContainer) {
        msgContainer.style.display = 'none';
        qrContainer.style.display = 'block';
      }
    }
  }
</script>
